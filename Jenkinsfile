pipeline{
  agent any
   stages {
     stage ('files') {
      steps {
        sh 'echo "asd" | sudo -S rm -rf /var/wwww/html/*'
        sh 'echo "asd" | sudo -S cp * /var/www/html/'
      }
     }
   }
}
